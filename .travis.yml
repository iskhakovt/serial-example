os:
  - linux
  - osx
language: cpp
compiler:
  - clang
  - gcc
install:
  - dependencies/build.sh
script:
  # https://github.com/travis-ci/travis-ci/issues/5434
  - set -eo pipefail
  # TODO: tests instead of hitting target
  - mkdir build && cd build && cmake .. && make && SerialExample
